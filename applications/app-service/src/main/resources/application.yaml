server:
  port: 8080
spring:
  application:
    name: "franchise"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
  data:
    mongodb:
      uri: "mongodb+srv://user:pxVBRiIOOyMMO4LQ@clustera.raasgjr.mongodb.net/franchise?retryWrites=true&w=majority"
  webflux:
    base-path: "/api"
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8080"
